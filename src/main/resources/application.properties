spring.datasource.url=jdbc:postgresql://localhost:5432/university
spring.datasource.username=postgres
spring.datasource.password=Reina0528*

# ===============================
# = JPA / HIBERNATE
# ===============================
# Mostrar o no registrar para cada consulta SQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.initialization-mode=always
logging.level.org.hibernate.sql=debug


# Mantenga viva la conexión si está inactiva durante mucho tiempo (necesaria en producción)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# Estrategia de naming
spring.jpa.hibernate.naming.implicit-strategy = org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyHbmImpl
spring.jpa.hibernate.naming.physical-strategy = org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy

# Permite a Hibernate generar SQL optimizado para un DBMS en particular
#pring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect